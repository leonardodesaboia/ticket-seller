# Marketplace Web — Local Development Environment Variables
# Copy this file to .env.local and adjust values as needed.
# NEVER commit .env.local.

# URL of the backend API (exposed to the browser — must start with NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:3000
